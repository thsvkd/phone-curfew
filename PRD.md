# PRD — 취침 커퓨 기록 앱

작성일 2026-09-08 · 상태 초안 · 대상 Android

---

## 1. 한 줄 정의

**새벽 2시에 잠들기로 한 약속을 지켰는지, 핸드폰 사용 기록으로 매일 판정해서 보여 주는 개인용 Android 앱.**

## 2. 배경과 문제

친구와 새벽 2시 취침을 약속했고, 그 수단으로 새벽 2시 이후에는 핸드폰을 쓰지 않기로 했다. 문제는 지켰는지 아닌지가 기억에만 남는다는 점이다. 아침에 일어나면 어젯밤 몇 시까지 핸드폰을 봤는지 정확히 떠오르지 않고, 며칠째 지키고 있는지는 더더욱 모른다.

기존 디지털 웰빙 앱은 하루 총 사용 시간과 앱별 순위를 보여 준다. 여기서 필요한 것은 다르다. **특정 시간대에 핸드폰을 켰는가 아닌가**라는 이진 판정과, 그 판정이 며칠째 이어지는지에 대한 기록이다.

## 3. 사용자

작성자 한 명. 개발자이며 Android 기기를 쓴다. 하루에 한 번, 주로 아침에 앱을 열어 어젯밤 결과를 확인한다. 앱을 오래 붙잡고 있을 이유가 없어야 하며, 열자마자 답이 보여야 한다.

## 4. 목표와 비목표

### 목표

- 어젯밤 커퓨를 지켰는지 앱을 열고 **1초 안에** 알 수 있다.
- 최근 일주일의 성공과 실패가 한눈에 보인다.
- 실패했다면 **몇 시에 얼마나** 썼는지 그래프에서 확인할 수 있다.
- 커퓨 시간대와 판정 기준을 언제든 바꿀 수 있고, 바꾸면 과거 기록도 새 기준으로 다시 채점된다.

### 비목표

이번 범위에서 명시적으로 만들지 않는 것들이다. 필요해지면 그때 별도로 결정한다.

- **핸드폰 사용 차단.** 이 앱은 막지 않는다. 기록하고 채점만 한다.
- **친구와의 공유, 계정, 서버, 동기화.** 데이터는 기기 안에서만 산다.
- **앱별 사용량 분석.** 어떤 앱을 썼는지는 판정에 쓰이지 않으므로 화면에도 없다.
- **목표 설정, 리워드, 연속 기록 게이미피케이션.** 연속 일수 표시는 하되 그 이상은 하지 않는다.
- **iOS 지원.** 아래 결정 D-1 참조.

## 5. 확정된 결정

| 번호 | 결정 | 근거 |
|---|---|---|
| D-1 | **Android 전용** | 요구한 10분 간격 사용 그래프는 Android의 `UsageStatsManager` 이벤트 스트림으로만 실제 데이터를 얻을 수 있다. iOS의 Screen Time API는 앱이 사용량 원본을 읽어 갈 수 없고 임계값 도달 콜백만 주므로, 같은 그래프를 그릴 수 없다. |
| D-2 | **차단하지 않고 기록·채점만** | 사용자가 요구한 기능은 "모니터링하여 보여 주는" 것 하나다. 차단은 접근성 서비스 권한과 스토어 심사, 우회 대응을 함께 끌고 들어오는 별개의 제품이다. |
| D-3 | **혼자 쓴다. 서버 없음** | 계정과 동기화를 빼면 백엔드가 통째로 사라진다. 개인 데이터가 기기를 벗어나지 않는 것도 이 앱의 성격에 맞다. |
| D-4 | **성공 판정에 허용 오차를 둔다** | 커퓨 시간대 누적 사용이 설정한 분 이하이면 성공으로 본다. 기본 5분. 알람 해제나 알림 확인 같은 짧은 조작까지 실패로 기록하면 지표가 쓸모없어진다. |
| D-5 | **디자인은 초안 4 "위젯 스택"** | 검토에서 채택. 둥근 카드를 세로로 쌓고, 시간대·주간 기록·그래프가 각자의 카드를 가지며, 라이트와 다크를 모두 지원한다. |

## 6. 기능 요구사항

우선순위는 **P0**(없으면 앱이 성립하지 않음), **P1**(첫 배포에 포함), **P2**(그다음)로 표기한다.

### F-1. 사용량 수집 · P0

기기가 사용된 구간을 10분 단위로 기록한다. 각 10분 칸에는 그 구간에서 화면을 켜고 실제로 조작한 시간이 분 단위(0–10)로 들어간다.

- 앱이 꺼져 있어도 백그라운드에서 수집이 이어진다.
- 수집이 한동안 멈췄다가 재개되면 밀린 구간을 메운다.
- 사용량 접근 권한이 없으면 수집할 수 없으므로, 첫 실행에서 권한을 요청하고 거부 상태에서는 그 사실을 화면에 계속 알린다.

### F-2. 커퓨 시간대 설정 · P0

시작 시각과 해제 시각을 각각 지정한다.

- 자정을 넘는 시간대(예: 23:30 → 07:00)를 지원한다.
- 매일 같은 시간대로 반복한다. 요일별로 다르게 두는 기능은 넣지 않는다.
- 변경하면 저장된 모든 과거 기록이 새 시간대로 즉시 다시 채점된다.

### F-3. 하루 판정 · P0

커퓨 창 하나가 곧 하루의 판정 단위다.

- 커퓨 창 안의 누적 사용이 허용 오차 이하이면 **성공**, 초과하면 **실패**.
- 아직 끝나지 않은 커퓨 창은 **진행 중**이며 성공도 실패도 아니다.
- 커퓨 창이 자정을 넘는 경우, 그 결과는 **창이 끝나는 날짜**(기상한 날)에 귀속한다.

### F-4. 최근 결과와 주간 기록 · P0

- 가장 최근에 **끝난** 커퓨 창의 결과를 성공 또는 실패로 크게 보여 준다.
- 최근 7일의 결과를 원으로 나열한다. 성공은 초록, 실패는 빨강, 진행 중은 중립색 테두리 원, 수집 공백 때문에 판정할 수 없는 날은 빈 점선 원.
- 연속 성공 일수와 이번 주 성공 일수를 함께 표시한다.

### F-5. 사용량 그래프 · P0

- 하루 24시간을 10분 간격 144칸으로 그린다.
- **선 그래프와 막대 그래프를 전환**할 수 있고, 선택은 다음 실행까지 유지된다.
- 세로축은 그 10분 중 화면을 켜 둔 분(0–10).
- 커퓨 시간대를 음영으로 겹쳐 그려서, 음영 안이 비어 있으면 성공임을 그래프만 보고 알 수 있다.
- 표시할 날짜를 하루씩 앞뒤로 넘길 수 있다.

### F-6. 허용 오차 설정 · P1

- 0분부터 30분까지 설정한다. 기본 5분.
- 변경하면 과거 기록이 즉시 다시 채점된다. F-2와 같은 동작이다.

### F-7. 기록 관리 · P2

- 수집된 기록을 CSV로 내보낸다.
- 전체 기록을 삭제한다.

## 7. 화면

초안 4를 기준으로 한 장짜리 앱이다. 세로로 쌓인 카드를 위에서부터 읽는다.

| 순서 | 카드 | 담는 것 |
|---|---|---|
| 0 | 머리글 | 가장 최근 결과 한 줄 |
| 1 | 사용 제한 시간대 | 시작·해제 시각 |
| 2 | 이번 주 | 주간 성공 일수, 연속 일수 배지, 원 7개 |
| 3 | 사용량 그래프 | 날짜, 선/막대 전환, 그래프, 요약 수치 |

설정 화면은 별도로 두지 않고, 허용 오차와 기록 관리는 머리글 오른쪽의 설정 버튼에서 열리는 화면 하나에 모은다.

## 8. 제품이 성공했다고 볼 조건

기능 목록과 별개로, 다음이 충족되어야 이 앱을 만든 의미가 있다.

1. 아침에 앱을 열면 **화면을 내리지 않고** 어젯밤 결과를 확인할 수 있다.
2. 표시된 판정이 실제 기억과 어긋나지 않는다. 실패를 성공으로 잘못 표시하는 일이 없어야 한다.
3. 앱을 며칠 열지 않아도 그동안의 기록이 비어 있지 않다.
4. 배터리 사용량 목록에서 이 앱이 눈에 띄지 않는다.

## 9. 위험과 제약

| 위험 | 내용 | 대응 방향 |
|---|---|---|
| 이벤트 보존 기간 | Android가 보관하는 사용 이벤트는 며칠치뿐이고 기기마다 다르다. 그 기간이 지나면 되돌아가 채울 수 없다. | 자체 저장소에 주기적으로 적재한다. 수집 공백은 그래프에 공백으로 표시하고 성공으로 오판하지 않는다. |
| 배터리 최적화 | 제조사의 절전 정책이 백그라운드 작업을 멈출 수 있다. | 수집 주기를 느슨하게 잡고, 앱을 열 때도 밀린 구간을 메운다. 오래 멈춘 것이 감지되면 알린다. |
| 권한 이탈 | 사용량 접근 권한은 사용자가 언제든 회수할 수 있다. | 매 수집 시 권한을 확인하고, 없으면 화면 상단에 지속적으로 표시한다. |
| "사용"의 정의 | 화면이 켜졌지만 잠금 상태이거나, 알림만 확인한 경우를 어떻게 셀지에 따라 판정이 달라진다. | SPEC에서 정의를 하나로 고정하고, 허용 오차(D-4)로 경계 사례를 흡수한다. |

## 10. 다음 문서

기술 구현, 데이터 모델, 판정 알고리즘, 화면별 세부 규격은 `SPEC.md`에서 정의한다.
