# SPEC — 취침 커퓨 기록 앱

작성일 2026-09-08 · 상위 문서 `PRD.md`

이 문서는 PRD에서 정의한 요구사항을 구현 가능한 규격으로 옮긴다. PRD가 "무엇을 왜"라면 여기서는 "어떻게"를 정한다.

---

## 1. 전제

| 항목 | 값 |
|---|---|
| 플랫폼 | Android 전용 (PRD D-1) |
| minSdk | 29 (Android 10) |
| targetSdk | 최신 안정 버전 |
| 언어 · UI | Kotlin · Jetpack Compose (Material 3) |
| 저장소 | Room (SQLite) |
| 백그라운드 | WorkManager |
| 네트워크 | 없음. `INTERNET` 권한을 선언하지 않는다 |
| 패키지 | `com.thsvkd.curfew` |

minSdk를 29로 잡은 이유는 아래 4장에서 쓰는 `UsageEvents.Event` 상수가 모두 그 이하에서 공개되었기 때문이다.

차트는 외부 라이브러리 없이 Compose `Canvas`로 직접 그린다. 필요한 것은 144개 점을 선 또는 막대로 찍는 일뿐이라 라이브러리를 얹을 이유가 없다.

## 2. 권한

| 권한 | 종류 | 용도 |
|---|---|---|
| `android.permission.PACKAGE_USAGE_STATS` | 특수 권한 | 사용량 이벤트 조회 |

선언하는 권한은 이 하나뿐이다. 재부팅 후 재개는 WorkManager가 스스로 하므로
`RECEIVE_BOOT_COMPLETED`가 필요 없고, 수집이 끊긴 사실은 알림 대신 화면 배너로 알리므로
`POST_NOTIFICATIONS`도 필요 없다. 근거는 각각 4.4와 4.5에 적었다.

`PACKAGE_USAGE_STATS`는 일반적인 런타임 요청으로 받을 수 없고, `Settings.ACTION_USAGE_ACCESS_SETTINGS` 인텐트로 시스템 설정 화면을 열어 사용자가 직접 켜야 한다.

허용 여부 확인은 `AppOpsManager`의 `OPSTR_GET_USAGE_STATS`를 `unsafeCheckOpNoThrow`로 조회한다. 앱이 포그라운드로 올라올 때마다 확인하며, 허용되어 있지 않으면 화면 최상단에 설정 화면으로 가는 배너를 띄운다.

## 3. "사용"의 정의

PRD 9장에서 남겨 둔 정의를 여기서 하나로 고정한다.

> **사용 중** = 화면이 상호작용 가능한 상태이면서 **동시에** 잠금이 해제되어 있는 상태.

이 정의에 따라 다음이 결정된다.

- 화면만 켜지고 잠금 화면에서 시간을 확인한 경우: **사용 아님.**
- 잠금을 풀고 알림을 확인한 뒤 곧바로 끈 경우: **사용임.** 다만 몇 초에서 몇십 초이므로 허용 오차(6장)가 흡수한다.
- Always-on 디스플레이나 알림으로 화면이 잠깐 밝아진 경우: **사용 아님.** `SCREEN_INTERACTIVE`는 앰비언트 상태를 포함하지 않는다.
- 음악을 틀어 두고 화면을 끈 경우: **사용 아님.** 취침 여부를 재는 것이 목적이므로 의도한 동작이다.

## 4. 사용량 수집

### 4.1 이벤트 소스

`UsageStatsManager.queryEvents(beginMs, endMs)`로 이벤트 스트림을 읽는다. `queryUsageStats`는 하루 또는 시간 단위 집계만 주므로 10분 해상도에 쓸 수 없다.

사용하는 이벤트 타입은 다음 여섯 가지다. 값은 AOSP `UsageEvents.java` 기준이다.

| 상수 | 값 | 상태 전이 |
|---|---|---|
| `SCREEN_INTERACTIVE` | 15 | `screenOn = true` |
| `SCREEN_NON_INTERACTIVE` | 16 | `screenOn = false` |
| `KEYGUARD_HIDDEN` | 18 | `unlocked = true` |
| `KEYGUARD_SHOWN` | 17 | `unlocked = false` |
| `ACTIVITY_RESUMED` | 1 | `unlocked = true` (보조 신호) |
| `ACTIVITY_PAUSED` | 2 | 상태를 바꾸지 않음. 진단용으로만 기록 |

`ACTIVITY_RESUMED`를 보조로 쓰는 이유는, 일부 기기에서 `KEYGUARD_HIDDEN`이 누락되기 때문이다. 잠금 상태에서는 일반 앱 액티비티가 재개되지 않으므로, 액티비티가 재개되었다는 것은 잠금이 풀렸다는 뜻이다.

### 4.2 이벤트를 사용 구간으로

이벤트를 시각 순으로 훑으면서 `screenOn && unlocked`가 참인 동안을 하나의 사용 구간으로 만든다.

```
for event in events (오름차순):
    wasUsing = screenOn && unlocked
    상태 전이 적용
    nowUsing = screenOn && unlocked
    if !wasUsing && nowUsing:  segmentStart = event.timeStamp
    if wasUsing && !nowUsing:  emit(segmentStart, event.timeStamp)
if screenOn && unlocked:       emit(segmentStart, cursorEnd)   // 열린 구간
```

마지막에 열린 채 남은 구간은 이번 수집의 끝 시각까지로 잘라 내보낸다. 이 처리가 없으면 15분마다
사용 구간이 끊긴다.

**창의 시작 상태는 이벤트로 유도하지 않고 기기에 직접 묻는다.** 이미 화면을 켜고 쓰는 중에 창이
시작되면 그 창 안에는 켜짐 이벤트가 없다. 시작 상태를 "꺼짐·잠금"으로 가정하는 순간 그 구간의
사용이 통째로 사라지고, 실패해야 할 날이 성공으로 뒤집힌다. PRD 8장 2번이 금지하는 바로 그
방향의 오차다. 실기기 첫 검증에서 실제로 이 증상이 나왔다.

그래서 수집이 끝날 때마다 `PowerManager.isInteractive`와 `KeyguardManager.isKeyguardLocked`를
읽어 그 값을 다음 창의 시작 상태로 저장한다. 창의 끝은 언제나 "지금"이므로 이 값이 이벤트로
유도한 상태보다 정확하고, 이벤트가 한 번 새더라도 어긋남이 한 창(15분)을 넘겨 이어지지 않는다.
첫 실행에서 커서를 세울 때도 같은 값을 읽는다.

### 4.3 10분 버킷 적재

사용 구간 `[start, end)`를 10분 경계로 잘라 겹치는 시간을 각 버킷에 **초 단위로** 누적한다. 분으로 반올림해서 저장하면 짧은 조작이 전부 사라지거나 부풀려져 허용 오차 판정이 어긋난다.

- 버킷 키 `bucketStartMs`: UTC 밀리초를 10분(600,000ms)으로 내림 정렬한 값.
- 값 `usedSeconds`: 0 이상 600 이하.
- 날짜와 시각 슬롯은 조회할 때 기기 로컬 타임존으로 변환해서 구한다. 로컬 날짜를 키에 넣지 않으므로 타임존이나 서머타임이 바뀌어도 저장된 값은 다시 쓸 수 있다. 서머타임 전환일에는 하루의 슬롯 수가 144가 아닐 수 있으며, 그래프는 그날 실제 슬롯 수만큼 그린다. 한국 표준시에는 서머타임이 없어 평상시에는 항상 144다.
- 같은 버킷에 다시 적재될 때는 더한다. 수집이 겹쳐 실행되어도 이중 계산되지 않도록, 커서보다 앞선 이벤트는 무시한다.

### 4.4 수집 스케줄

| 시점 | 방식 |
|---|---|
| 정기 | `PeriodicWorkRequest` 15분 간격. WorkManager가 허용하는 최소 주기다 |
| 앱 진입 | 앱이 포그라운드로 올라올 때 `OneTimeWorkRequest` 1회 |

재부팅을 위한 처리는 따로 두지 않는다. WorkManager는 등록된 정기 작업을 자체 저장소에 남기고
기기가 다시 켜지면 스스로 복원하므로, `BOOT_COMPLETED` 수신기를 두면 같은 일을 두 번 하는
셈이 된다.

수집 창은 `[lastCursorMs, now)`이며, 끝나면 `lastCursorMs = now`로 갱신한다. 절전 정책 때문에 작업이 몇 시간 밀려도 다음 실행이 밀린 구간을 한 번에 처리한다.

### 4.5 수집 공백

Android가 보관하는 이벤트는 며칠치뿐이라, 수집이 오래 멈추면 되돌아가 채울 수 없는 구간이 생긴다. 이 구간을 **공백**으로 명시적으로 기록해서, 사용량 0과 구분한다.

공백 판정: `queryEvents(lastCursorMs, now)`가 돌려준 **첫 이벤트의 시각**이 `lastCursorMs`보다 유의미하게 뒤라면, 그 사이는 이벤트가 이미 삭제된 구간일 수 있다. 다만 실제로 아무 일도 없었을 수도 있으므로 다음 기준으로 나눈다.

- `now - lastCursorMs <= 6시간`: 공백으로 보지 않는다. 정기 작업이 지연된 정상 범위다.
- `now - lastCursorMs > 6시간`: `[lastCursorMs, 첫 이벤트 시각)`을 공백으로 기록한다.

앱을 처음 설치한 시점 이전도 공백으로 간주한다.

마지막 수집으로부터 24시간이 지나도록 커서가 전진하지 않았다면, 앱 화면 상단에 배너를 띄워
절전 설정을 확인하도록 안내한다. 알림이 아니라 배너인 이유는 사용자가 아침마다 앱을 열어
어젯밤 결과를 확인하기 때문이다(PRD 3장). 그 자리에서 보이는 경고면 충분하고, 알림 채널과
런타임 권한을 끌고 들어올 이유가 없다.

## 5. 데이터 모델

Room 엔티티 세 개와 설정 저장소 하나로 끝난다. 판정 결과는 저장하지 않는다. 커퓨 시간대나 허용 오차가 바뀌면 통째로 다시 계산해야 하는데, 하루 144행 기준 일주일이 1,008행이라 조회할 때마다 계산해도 즉시 끝나기 때문이다. 캐시 테이블을 두면 무효화 로직만 늘어난다.

### `usage_bucket`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `bucketStartMs` | INTEGER, PK | 10분 정렬된 UTC 밀리초 |
| `usedSeconds` | INTEGER | 0–600 |

### `collector_state` (단일 행, id 고정)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `lastCursorMs` | INTEGER | 다음 수집 시작점 |
| `screenOn` | INTEGER | 마지막으로 관측한 화면 상태 |
| `unlocked` | INTEGER | 마지막으로 관측한 잠금 상태 |

넘길 값은 불린 두 개면 충분하다. 사용 구간은 항상 수집 창 안으로 잘려 나가므로, 창 끝에서
열려 있던 구간은 이미 그 창의 끝까지 기록되어 있고 다음 창은 시작 시각부터 다시 이어 붙이면
된다. 열린 구간의 시작 시각을 따로 저장할 필요가 없다.

두 불린에는 이벤트로 유도한 값이 아니라 수집이 끝난 시점에 기기에서 직접 읽은 값이 들어간다.
근거는 4.2에 적었다.

### `coverage_gap`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `startMs` | INTEGER, PK | 공백 시작 |
| `endMs` | INTEGER | 공백 끝 |

### 설정 (DataStore Preferences)

| 키 | 타입 | 기본값 |
|---|---|---|
| `curfewStartMinutes` | Int | 120 (02:00) |
| `curfewEndMinutes` | Int | 420 (07:00) |
| `toleranceSeconds` | Int | 300 (5분) |
| `chartMode` | String | `line` |

시각은 자정 기준 분으로 저장한다. 0 이상 1439 이하.

### 보존

기록은 자동으로 지우지 않는다. 하루 144행이므로 1년을 모아도 5만여 행이고 SQLite에게는 아무 부담이 아니다. 삭제는 사용자가 설정에서 명시적으로 한다 (PRD F-7).

## 6. 판정

### 6.1 커퓨 창

날짜 `D`(로컬 날짜)의 커퓨 창을 다음과 같이 정한다.

```
end   = D일 00:00 + curfewEndMinutes
start = end - windowLength
windowLength = (curfewEnd - curfewStart + 1440) mod 1440
```

`windowLength`가 0이면 커퓨가 설정되지 않은 것으로 보고 판정하지 않는다.

이 정의는 PRD F-3의 "창이 끝나는 날짜에 귀속한다"를 그대로 옮긴 것이다. 02:00–07:00이면 창이 같은 날 안에서 닫히고, 23:30–07:00이면 전날 23:30에 시작해 당일 07:00에 닫히면서 결과는 당일에 붙는다. 어느 쪽이든 "아침에 일어나서 어젯밤을 확인한다"는 사용 방식과 어긋나지 않는다.

### 6.2 판정 상태

| 상태 | 조건 |
|---|---|
| `IN_PROGRESS` | `now < end`. 창이 아직 닫히지 않았다 |
| `NO_DATA` | 창이 `coverage_gap`과 조금이라도 겹친다 |
| `SUCCESS` | 창 안 `usedSeconds` 합계 `<= toleranceSeconds` |
| `FAIL` | 그 밖 |

우선순위는 표의 순서와 같다. 진행 중인지 먼저 보고, 그다음 데이터가 성한지 보고, 마지막에 채점한다. 공백을 성공으로 오판하지 않기 위한 순서다.

### 6.3 부분 버킷

커퓨 창의 경계가 10분 슬롯 가운데를 지나는 경우(예: 시작이 23:35)에는 그 버킷을 통째로 세지 않고 겹치는 비율만큼 안분한다.

```
weight = (버킷과 창이 겹치는 밀리초) / 600000
기여 = usedSeconds * weight
```

버킷 안에서 사용이 고르게 분포한다는 가정이며, 10분 해상도에서 그 이상 정밀하게 나눌 근거가 없다. 설정에서 시각을 10분 단위로만 고르게 하면 이 계산 자체가 필요 없어지지만, 취침 시각을 23:30이나 01:45로 두고 싶을 수 있어 분 단위 입력을 허용하고 안분으로 처리한다.

### 6.4 재채점

커퓨 시간대나 허용 오차가 바뀌면 별도 마이그레이션 없이 다음 조회부터 새 값이 적용된다. 5장에서 결과를 저장하지 않기로 한 결과다.

## 7. 화면

PRD D-5에 따라 초안 4 "위젯 스택"을 구현한다. 목업은 `mockups/d4.html`이며, 아래는 그것을 Compose로 옮기기 위한 규격이다.

### 7.1 색 토큰

| 토큰 | 라이트 | 다크 |
|---|---|---|
| `background` | `#F4F2FB` | `#141220` |
| `card` | `#FFFFFF` | `#1F1B2D` |
| `onSurface` | `#282237` | `#EFECF8` |
| `onSurfaceMuted` | `#7E7695` | `#9E96B8` |
| `outline` | `#EAE6F4` | `#2C2740` |
| `accent` | `#6C4FE0` | `#A98DFF` |
| `accentContainer` | `#EDE8FD` | `#2A2340` |
| `success` | `#2FA96A` | `#4FCB8B` |
| `failure` | `#E0474C` | `#FF7A7E` |

`success`와 `failure`는 강조색과 별개의 의미색이다. 강조색을 성공에 재사용하지 않는다.

- 서체: Gothic A1. 본문 400, 강조 700, 제목 800.
- 카드: 모서리 반경 20dp, 안쪽 여백 18dp, 카드 사이 간격 14dp, 화면 좌우 여백 18dp.
- 숫자가 세로로 정렬되는 자리에는 고정폭 숫자를 쓴다.

### 7.2 카드 구성

**머리글**

- 제목과, 가장 최근에 끝난 커퓨 창의 결과 한 줄.
- 오른쪽에 설정 화면으로 가는 버튼.
- 권한이 없거나 공백이 24시간 이상이면 이 아래에 경고 배너를 넣는다.

**카드 1 — 사용 제한 시간대**

- 시작과 해제 두 칸. 각각 눌러 `TimePickerDialog`를 연다.
- 24시간 표기로 보여 준다. 목업에서 확인했듯 시스템 시각 입력은 로케일에 따라 "오전/오후"를 붙여 자리를 크게 차지하므로, 표시는 앱이 직접 그린다.
- 시간대 길이가 0이 되는 입력은 저장하지 않고 그 자리에서 알린다.

**카드 2 — 이번 주**

- `N / 7일 성공`과 연속 일수 배지.
- 원 7개. 지름 22dp, 오늘 자리에는 강조색 링을 두른다.
  - `SUCCESS` 채운 초록, `FAIL` 채운 빨강, `IN_PROGRESS` 테두리만 있는 중립색, `NO_DATA` 점선 테두리에 속은 비움.
- 각 원 아래 요일 한 글자.
- 원을 누르면 카드 3의 그래프가 그날로 이동한다.

**카드 3 — 사용량 그래프**

- 머리에 날짜와 좌우 이동 버튼, 오른쪽에 선/막대 세그먼트 토글.
- 그래프 본체는 7.3.
- 아래 요약 세 개: 하루 총 사용, 커퓨 시간대 사용, 표본 간격 안내.

### 7.3 그래프 렌더링

`Canvas`에 직접 그린다. 좌표는 목업과 같은 비율을 쓴다.

| 항목 | 규격 |
|---|---|
| 가로 | 하루의 슬롯 수만큼 균등 분할 (평상시 144) |
| 세로 | 0–10분 고정. 가로 눈금 0, 5, 10 |
| 커퓨 음영 | 강조색 10% 불투명도. 자정을 넘으면 좌우 두 덩어리로 나뉜다 |
| 공백 구간 | 중립색 28% 불투명도의 띠. 커퓨 음영은 강조색이라 색상만으로 갈린다 |
| 아직 오지 않은 시각 | 공백과 같이 덮는다. 사용 0분으로 그리면 하루를 이미 다 보낸 것처럼 읽힌다 |
| 선 모드 | 굵기 2dp, 이음새와 끝을 둥글게. 아래 면적은 강조색 15% |
| 막대 모드 | 슬롯 폭의 70%, 값이 0인 슬롯은 그리지 않음 |
| 가로 축 라벨 | 00:00, 06:00, 12:00, 18:00, 24:00 |
| 텍스트 색 | `onSurfaceMuted`. 라이트와 다크 모두에서 읽혀야 한다 |

선택한 모드는 `chartMode`에 저장해 다음 실행에서 복원한다.

### 7.4 설정 화면

- 허용 오차 슬라이더. 0–30분, 1분 단위, 기본 5분.
- CSV 내보내기 (`bucketStartMs`, 로컬 시각, `usedSeconds` 세 열).
- 전체 기록 삭제. 되돌릴 수 없으므로 확인 대화상자를 거친다.
- 사용량 접근 권한 상태와 설정 화면 바로가기.

## 8. 엣지 케이스

| 상황 | 처리 |
|---|---|
| 커퓨 창이 자정을 넘음 | 6.1의 정의로 흡수. 결과는 창이 끝나는 날짜에 귀속 |
| 커퓨 창 길이 0 | 판정하지 않음. 설정에서 저장을 막음 |
| 창 경계가 슬롯 중간 | 6.3 안분 |
| 기기 재부팅 | `collector_state`가 남아 있으므로 커서에서 이어받는다. 부팅 직후 상태는 화면 꺼짐·잠금으로 초기화 |
| 시각 수동 변경, 타임존 이동 | 버킷 키가 UTC라 저장값은 그대로. 날짜 귀속만 바뀐다 |
| 서머타임 전환일 | 그날 슬롯 수가 144가 아닐 수 있고, 그래프는 실제 슬롯 수만큼 그린다 |
| 권한 회수 후 재허용 | 회수 기간은 공백으로 남고 `NO_DATA`로 판정된다 |
| 이벤트가 하나도 없는 수집 | 커서만 전진. 화면을 한 번도 켜지 않은 경우와 같다 |
| 수집 작업 중복 실행 | 커서보다 앞선 이벤트를 무시하므로 이중 계산되지 않음 |
| 설치 이전 기간 조회 | 공백으로 표시 |
| 기록 전체 삭제 | 삭제 시점 이전을 공백 한 건으로 남긴다. 지웠다는 사실과 그때 실제로 안 썼다는 사실을 뒤섞지 않기 위해서다 |

## 9. 테스트

수집기와 판정기는 Android 프레임워크에 기대지 않는다. 수집기의 입력은 이벤트 목록이고 출력은
칸 목록이며, 판정기의 입력은 칸과 공백과 설정값이다. 그래서 전부 계측 없이 도는 JVM 단위
테스트로 덮을 수 있고, 실제로 `app/src/test`에 그렇게 두었다.

**수집기** (`UsageCollectorTest`)

1. 화면과 잠금이 모두 풀려야 사용 구간이 시작된다.
2. `KEYGUARD_HIDDEN`이 없어도 액티비티 재개를 잠금 해제로 받는다.
3. 화면만 켜지고 잠금이 남아 있으면 사용이 아니다.
4. 칸 경계를 넘는 구간은 두 칸에 나뉘고 합이 보존된다.
5. 열린 구간이 다음 수집으로 이어지며 총합이 끊기지 않는다.
6. 커서보다 앞선 이벤트를 다시 넣어도 값이 늘지 않는다.
7. 창이 시작될 때 이미 사용 중이면 창 시작부터 센다. 실기기 검증에서 잡힌 결함의 회귀 테스트다.

**판정기** (`CurfewTest`)

8. 자정을 넘는 커퓨의 길이를 구한다.
9. 커퓨 길이가 0이면 창이 없다.
10. 자정을 넘는 창은 끝나는 날짜에 귀속한다.
11. 제한 시간대 사용이 없으면 성공이다.
12. 허용 오차 경계에서 성공과 실패가 갈린다. 300초는 성공, 301초는 실패.
13. 창이 공백과 겹치면 사용량과 무관하게 판정하지 않는다.
14. 창 밖의 공백은 판정에 영향을 주지 않는다.
15. 아직 끝나지 않은 창은 진행 중이다.
16. 창 경계가 칸 가운데를 지나면 겹치는 비율만큼만 센다.

**기기 통합** (`OvernightE2ETest`, `androidTest`)

이 셋은 밤을 기다리지 않고 실기기에서 일주일치를 재현한다. 정답지가 되는 사용 구간을 먼저
정하고, 거기서 시스템이 내보낼 이벤트를 만든 다음, 워커와 같은 15분 창으로 쪼개어 수집을
반복한다. 저장은 진짜 Room 데이터베이스에 한다. 단위 테스트가 덮지 못하던 DAO의 삽입 후 누적
SQL, 창 경계에서의 이중 계산, 수집부터 판정까지의 연쇄가 여기서 처음 검증된다.

17. 일주일치 밤을 15분 주기로 수집해 판정까지 재현한다. 성공·실패·허용 오차 양쪽 경계·수집
    공백을 모두 포함한 이레를 넣고, 하루 총 사용과 커퓨 시간대 사용이 정답지와 정확히 같은지,
    일곱 날의 판정과 주간 성공 일수·연속 일수가 기대와 같은지 확인한다.
18. 아직 끝나지 않은 창은 사용량과 무관하게 진행 중이다.
19. 한 칸에 10분을 넘겨 담아도 600초에서 멈춘다.

**수동 확인**

20. 실기기에서 하룻밤을 지낸 뒤, 그래프의 사용 구간이 실제 기억과 맞는지 확인한다.
    PRD 8장 2번 조건에 해당하며 자동화할 수 없다.

## 10. 마일스톤

| 단계 | 내용 | 완료 판단 |
|---|---|---|
| M1 | 권한 요청, 수집기, Room 적재, WorkManager 등록 | 하루 돌린 뒤 `usage_bucket`이 실제 사용과 맞는다. 테스트 1–5 통과 |
| M2 | 판정기, 머리글, 카드 2 | 테스트 6–11 통과. 아침에 열면 어젯밤 결과가 보인다 |
| M3 | 카드 3 그래프, 선/막대 전환, 날짜 이동 | 그래프가 라이트와 다크 모두에서 읽히고 공백이 0과 구분된다 |
| M4 | 설정 화면, 허용 오차, CSV 내보내기, 삭제 | PRD F-6, F-7 충족 |

M2까지가 PRD 8장의 성공 조건을 만족하는 최소 앱이다. M3와 M4는 그 위에 얹는다.

## 11. 열어 둔 것

다음은 지금 결정하지 않아도 진행에 지장이 없어 미뤄 둔다.

- 앱 표시 이름과 아이콘.
- 위젯이나 타일로 결과를 내보내는 기능. 앱을 여는 것 자체가 부담이 되면 그때 검토한다.
- 커퓨 창을 요일별로 다르게 두는 기능. PRD F-2에서 명시적으로 뺐다.
